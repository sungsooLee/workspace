import React from 'react';

const VideoPlayer: React.FC = () => {
  //   const videoUrl = '/video/test.mp4';

  return (
    <div>
      {/* <video width='600' controls>
        <source src={videoUrl} type='video/mp4' />
      </video> */}
    </div>
  );
};

export default VideoPlayer;
