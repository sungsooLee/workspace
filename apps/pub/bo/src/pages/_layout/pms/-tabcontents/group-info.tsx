import { FC } from 'react';
import { cn } from '@learnway/shared';
// eslint-disable-next-line no-empty-pattern
const GroupInfoComponent: FC<{}> = ({}) => {
  return (
    <div>
      <div>{/* 컨텐츠 영역 */}</div>
    </div>
  );
};

GroupInfoComponent.displayName = 'GroupInfo';
export const GroupInfo = GroupInfoComponent;
