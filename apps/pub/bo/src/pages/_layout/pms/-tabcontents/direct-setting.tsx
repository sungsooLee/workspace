import { FC } from 'react';
import { cn } from '@learnway/shared';
// eslint-disable-next-line no-empty-pattern
const DirectSettingComponent: FC<{}> = ({}) => {
  return <div></div>;
};

DirectSettingComponent.displayName = 'DirectSetting';
export const DirectSetting = DirectSettingComponent;
