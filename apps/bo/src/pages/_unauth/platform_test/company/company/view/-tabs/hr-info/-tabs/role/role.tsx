import { useTranslation } from 'react-i18next';
import React from 'react';

const RoleComponent = () => {
  const { t } = useTranslation();
  return <div className={'flex flex-col'}>role.tsx</div>;
};

export const Role = RoleComponent;
