import { useTranslation } from 'react-i18next';
import React from 'react';

const PositionComponent = () => {
  const { t } = useTranslation();
  return <div className={'flex flex-col'}>position.tsx</div>;
};

export const Position = PositionComponent;
