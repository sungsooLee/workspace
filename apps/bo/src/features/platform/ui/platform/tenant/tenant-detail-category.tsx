import { FC, useState } from 'react';
import { t } from 'i18next';
import {
  Button,
  ContentsRow,
  Textarea,
  Switch,
  Tooltip,
  Input,
  TreeView,
  TreeNode,
} from '@learnway/ui';

const TenantDetailCategoryComponent: FC<any> = ({ menuScope }) => {
  return 'tenant category mapping';
};

export const TenantDetailCategory = TenantDetailCategoryComponent;
